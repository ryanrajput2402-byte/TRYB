// Group F, item 11 — shared label text so the same three options read
// identically wherever they're shown (organizer card, self profile, other
// user's profile).
export const RESPONSE_TIME_LABELS: Record<string, string> = {
  fast: "⚡ Usually responds within hours",
  daily: "📅 Usually responds within a day",
  flexible: "🌤 Response time varies",
};
